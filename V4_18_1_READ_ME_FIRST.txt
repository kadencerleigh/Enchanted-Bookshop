Enchanted Bookshop V4.18.1 — Live Oracle Filters

Patch built from V4.18 TBR Oracle.

Changes:
- TBR Oracle cauldron count updates immediately when Genre, Length, Spice, Series, or Favorites changes.
- Mood also updates live (it remains a soft preference and does not reduce the hard-filter cauldron count).
- If a new filter makes the current prophecy invalid, that result is cleared automatically.
- Genre filtering remains exact: Romance only matches books that actually have Romance saved as a genre.
- No SQL/schema changes.
